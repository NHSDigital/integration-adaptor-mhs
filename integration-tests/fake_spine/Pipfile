[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
tornado = "~=6.0"
pystache = "~=0.6"
integration-adaptors-common = {editable = true, path = "../../common"}

[requires]
python_version = "3.7"

[scripts]
start = "python main.py"
uninstall_setuptools = "pip uninstall -y setuptools"
# Change this version number if upgrading from Python 3.7 to 3.8
install_setuptools = "pip install -Iv setuptools==68.0.0"